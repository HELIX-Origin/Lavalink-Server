# Lavalink Server Configuration
ADMIN_KEY=""                                                 # Optional host owner dashboard password (defaults to LAVA_PASS if empty)
PUBLIC_URL=""                                                # Optional public domain/host for the server (e.g. lavalink.yourdomain.com)

# YouTube & Remote Cipher
YOUTUBE_CLIENT_ID=""                                         # REQUIRED - YouTube OAuth Client ID (app type: "TVs and Limited Input devices")
YOUTUBE_CLIENT_SECRET=""                                     # YouTube OAuth Client Secret for the Client ID above (needed for the token exchange)

YOUTUBE_REFRESH_TOKEN=""                                     # Optional - Pre-authorized refresh token (auto-saved after device flow)

# Spotify
SPOTIFY_CLIENT_ID=""                                         # Spotify Developer App Client ID
SPOTIFY_CLIENT_SECRET=""                                     # Spotify Developer App Client Secret

# Keep-Alive & Anti-Throttling
KEEP_ALIVE_ENABLED=true                                      # Set to true to ping /health to keep the node active
KEEP_ALIVE_INTERVAL_MS=300000                                # Ping interval in ms (default: 300000 = 5 minutes)
